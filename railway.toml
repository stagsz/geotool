[build]
dockerfilePath = "render-service/Dockerfile"

[deploy]
startCommand = "node render-service/dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
